//const rota  = 'https://raw.githubusercontent.com/victorfort/bdJson/main/bdParaColecione/data.json'
/* async function readDataFromGithub() {
    try {
        const response = await fetch('../../back/data.json');
        if (!response.ok) {
            throw new Error(`Erro HTTP! status: ${response.status}`);
        }
        const data = await response.json();
        console.log(data); // Aqui você já terá os dados do JSON
    } catch (error) {
        console.error('Erro ao carregar o arquivo JSON:', error);
    }
}





function addEventBtnTrash() {
    document.querySelectorAll('.btnTrashIten').forEach(button => {
        button.addEventListener('click', function() {
            const id = this.getAttribute('data-id');
            console.log('clicou no lixeiro do item ', id);
        });
    });
}
 */


